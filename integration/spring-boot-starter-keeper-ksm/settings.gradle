pluginManagement {
  repositories {
    // so Gradle can still resolve community plugins if you ever need them
    gradlePluginPortal()
  }
}

plugins {
  id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'spring-boot-starter-keeper-ksm'
